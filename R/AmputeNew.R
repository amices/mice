
#'@export
ampute <- function(data, patterns, prop = 0.5, bycases = TRUE, run = TRUE) {
  
  
  
}
                   
pattern <- list(pat = c(0, 1, 1, 1),
                mech = "MAR", 
                weights = c(0, 1, 1, 1), 
                cont = "TRUE", 
                type = "MARRIGHT", 
                size = 0.5)